# Инструкция по тестированию новых функций

Этот документ содержит инструкции по тестированию всех новых функций, реализованных в ChatFlow.

## ⚡ Быстрый старт (автоматическое тестирование)

**Просто выполните одну команду:**

```bash
# Windows
npm run test:new-features

# Linux/macOS
npm run test:new-features:bash
```

Это автоматически запустит все тесты и выведет подробный отчет. **Просто скопируйте вывод и отправьте для анализа!**

Для тестирования только Excel API (нужен запущенный сервер):
```bash
npm run test:excel-api
```

---

## Содержание

1. [Тестирование работы с Excel файлами](#тестирование-работы-с-excel-файлами)
2. [Тестирование Client Tools (Function Calling)](#тестирование-client-tools-function-calling)
3. [Тестирование диалога подтверждения](#тестирование-диалога-подтверждения)
4. [Тестирование меншонов ячеек](#тестирование-меншонов-ячеек)
5. [Интеграционное тестирование](#интеграционное-тестирование)

---

## Тестирование работы с Excel файлами

### Предварительные требования

1. Убедитесь, что установлены все зависимости:
   ```bash
   npm install
   ```

2. Убедитесь, что база данных инициализирована:
   ```bash
   npm run migrate
   ```

3. **Подготовьте тестовый Excel файл** (обязательно):
   
   Для полноценного тестирования функций работы с Excel необходим тестовый файл. Вы можете:
   
   **Вариант 1: Создать простой тестовый файл вручную**
   - Создайте новый файл в Excel, LibreOffice Calc или Google Sheets
   - Добавьте заголовки в первую строку (например: "Имя", "Возраст", "Город")
   - Добавьте несколько строк данных (минимум 5-10 строк для базового тестирования)
   - Сохраните как `.xlsx` или `.csv`
   
   **Пример структуры тестового файла:**
   ```
   Имя    | Возраст | Город
   Иван   | 25      | Москва
   Мария  | 30      | СПб
   Петр   | 28      | Казань
   Анна   | 32      | Москва
   ...
   ```
   
   **Вариант 2: Использовать готовый файл**
   - Используйте любой существующий Excel файл с данными
   - Рекомендуется файл с различными типами данных (текст, числа, даты)
   
   **Вариант 3: Использовать готовый скрипт (рекомендуется)**
   ```bash
   npm run create-test-excel
   ```
   Это создаст файл `test-data.xlsx` в корне проекта с готовыми тестовыми данными (10 строк с данными о сотрудниках).
   
   Вы можете указать свой путь:
   ```bash
   node scripts/create-test-excel.js path/to/your-file.xlsx
   ```
   
   **Вариант 4: Создать файл программно (для разработчиков)**
   ```javascript
   // Пример создания тестового файла через Node.js скрипт
   const XLSX = require('xlsx');
   const data = [
     ['Имя', 'Возраст', 'Город'],
     ['Иван', 25, 'Москва'],
     ['Мария', 30, 'СПб'],
     ['Петр', 28, 'Казань'],
   ];
   const ws = XLSX.utils.aoa_to_sheet(data);
   const wb = XLSX.utils.book_new();
   XLSX.utils.book_append_sheet(wb, ws, 'Sheet1');
   XLSX.writeFile(wb, 'test-data.xlsx');
   ```

---

## 🚀 Быстрый старт: Автоматическое тестирование

**Для максимальной автоматизации используйте готовые скрипты:**

### Полное автоматическое тестирование (рекомендуется)

**Windows (PowerShell):**
```bash
npm run test:new-features
```

**Linux/macOS (Bash):**
```bash
npm run test:new-features:bash
```

Эти команды автоматически:
1. ✅ Проверят зависимости
2. ✅ Создадут тестовый Excel файл
3. ✅ Применят миграции БД
4. ✅ Проверят типы TypeScript
5. ✅ Запустят сервер (если не запущен)
6. ✅ Протестируют все Excel API endpoints
7. ✅ Запустят unit тесты
8. ✅ Выведут детальный отчет с результатами

**Результат:** Вы получите отчет с цветной разметкой, который можно легко пересылать для анализа.

### Тестирование только Excel API

Если нужно протестировать только API endpoints:

```bash
npm run test:excel-api
```

**Требования:**
- Сервер должен быть запущен (`npm run dev`)
- Тестовый файл должен существовать (`npm run create-test-excel`)

### Что делать с результатами

После выполнения скрипта вы получите отчет вида:
```
╔═══════════════════════════════════════════════════════════╗
║                      Итоги тестирования                   ║
╚═══════════════════════════════════════════════════════════╝

Всего тестов: 7
Пройдено: 7
Провалено: 0
Процент успеха: 100.0%

✅ Все тесты пройдены успешно!
```

**Просто скопируйте вывод команды и отправьте его для анализа!**

### 1. Тестирование API загрузки Excel файлов

#### Тест 1: Загрузка валидного Excel файла

**Шаги:**
1. Подготовьте тестовый Excel файл (`.xlsx`, `.xls`, или `.csv`) - см. предварительные требования выше
2. Запустите сервер разработки:
   ```bash
   npm run dev
   ```
3. Используйте curl или Postman для отправки POST запроса:
   ```bash
   curl -X POST http://localhost:3000/api/excel/upload \
     -F "file=@path/to/your/test-file.xlsx"
   ```

**Ожидаемый результат:**
- HTTP статус 200
- JSON ответ с полями:
  - `success: true`
  - `fileId: <число>`
  - `metadata: { filename, size, rowCount, columnCount, headers, ... }`
  - `result: { rowsProcessed, rowsSaved, rowsWithErrors }`

#### Тест 2: Загрузка невалидного файла

**Шаги:**
1. Попробуйте загрузить файл с неподдерживаемым форматом (например, `.pdf`)

**Ожидаемый результат:**
- HTTP статус 400
- Сообщение об ошибке валидации типа файла

#### Тест 3: Загрузка слишком большого файла

**Шаги:**
1. Попробуйте загрузить файл больше 100MB (по умолчанию)

**Ожидаемый результат:**
- HTTP статус 500
- Сообщение об ошибке превышения максимального размера

### 2. Тестирование API получения данных Excel

#### Тест 1: Получение метаданных и данных файла

**Шаги:**
1. Загрузите Excel файл и получите `fileId` из ответа
2. Выполните GET запрос:
   ```bash
   curl http://localhost:3000/api/excel/<fileId>
   ```

**Ожидаемый результат:**
- HTTP статус 200
- JSON с полями:
  - `success: true`
  - `metadata: { fileId, filename, size, rowCount, columnCount, headers, uploadedAt }`
  - `data: Array<Record<string, unknown>>` - массив строк данных
  - `pagination: { offset, limit, total }`

#### Тест 2: Пагинация данных

**Шаги:**
1. Для файла с большим количеством строк выполните запрос с параметрами:
   ```bash
   curl "http://localhost:3000/api/excel/<fileId>?offset=10&limit=20"
   ```

**Ожидаемый результат:**
- Возвращается только 20 строк, начиная с 11-й строки
- `pagination.offset = 10`, `pagination.limit = 20`

#### Тест 3: Получение несуществующего файла

**Шаги:**
1. Попробуйте получить файл с несуществующим ID:
   ```bash
   curl http://localhost:3000/api/excel/99999
   ```

**Ожидаемый результат:**
- HTTP статус 404
- Сообщение об ошибке "Excel file with id 99999 not found"

### 3. Тестирование API списка файлов

**Шаги:**
```bash
curl http://localhost:3000/api/excel/list
```

**Ожидаемый результат:**
- HTTP статус 200
- JSON с полями:
  - `success: true`
  - `files: Array<ExcelFileMetadata>` - массив всех загруженных файлов
  - `count: <число>` - количество файлов

### 4. Тестирование API удаления файла

**Шаги:**
1. Загрузите файл и получите `fileId`
2. Удалите файл:
   ```bash
   curl -X DELETE http://localhost:3000/api/excel/<fileId>
   ```

**Ожидаемый результат:**
- HTTP статус 200
- JSON с полями:
  - `success: true`
  - `message: "Excel file <fileId> deleted successfully"`

3. Попробуйте получить удаленный файл:
   ```bash
   curl http://localhost:3000/api/excel/<fileId>
   ```

**Ожидаемый результат:**
- HTTP статус 404

### 5. Тестирование UI компонента ExcelViewer

**Предварительно:**
- Убедитесь, что у вас есть тестовый Excel файл (см. раздел "Предварительные требования" выше)
- Загрузите файл через API или используйте компонент ExcelUpload, чтобы получить `fileId`

**Шаги:**
1. Откройте приложение в браузере: http://localhost:3000
2. Создайте или выберите тред
3. Загрузите Excel файл через API (`POST /api/excel/upload`) или используйте компонент ExcelUpload
4. Запомните `fileId` из ответа API (понадобится для открытия файла)
5. Откройте ExcelViewer с помощью client tools (попросите AI показать файл) или передайте `fileId` напрямую в компонент

**Что проверить:**
- ✅ Файл отображается в виде таблицы
- ✅ Заголовки столбцов отображаются в первой строке
- ✅ Номера строк отображаются в первом столбце
- ✅ Буквенные индексы столбцов (A, B, C, ...) отображаются рядом с заголовками
- ✅ Клик по ячейке выделяет её (синий фон, рамка)
- ✅ При выделении ячейки показывается её ссылка (A1, B5 и т.д.) внизу
- ✅ Пагинация работает для больших файлов
- ✅ Кнопки "Предыдущая" и "Следующая" корректно переключают страницы
- ✅ Кнопка "Закрыть" закрывает просмотрщик
- ✅ Клавиатурная навигация работает (Enter/Space для выбора ячейки, Escape для закрытия)

### 6. Тестирование компонента ExcelUpload

**Шаги:**
1. Добавьте компонент ExcelUpload в UI (например, в главную страницу)
2. Попробуйте разные способы загрузки:

**Тест 1: Drag & Drop**
- Перетащите Excel файл в область загрузки
- Проверьте визуальную обратную связь (изменение цвета границы)
- Отпустите файл

**Ожидаемый результат:**
- Файл загружается
- Показывается индикатор загрузки
- После успешной загрузки вызывается `onUploadSuccess`

**Тест 2: Выбор файла через диалог**
- Кликните на область загрузки
- Выберите файл через диалог

**Ожидаемый результат:**
- Файл загружается
- Поведение аналогично drag & drop

**Тест 3: Невалидный файл**
- Попробуйте загрузить файл с неподдерживаемым форматом

**Ожидаемый результат:**
- Ошибка отображается через `onUploadError`
- Сообщение об ошибке понятное для пользователя

---

## Тестирование Client Tools (Function Calling)

### Предварительные требования

1. Убедитесь, что OpenAI API ключ настроен в `.env.local`:
   ```
   OPENAI_API_KEY=your_api_key_here
   ```

2. Убедитесь, что tools подключены в API route (должны быть в `app/api/chat/route.ts`)

### 1. Тестирование инструмента showConfirmation

**Шаги:**
1. Откройте чат в браузере
2. Отправьте сообщение, которое должно вызвать диалог подтверждения, например:
   - "Удали файл с ID 1"
   - "Измени данные в ячейке A1"
   - Любое действие, требующее подтверждения

**Ожидаемый результат:**
- AI определяет, что действие требует подтверждения
- Вызывается tool `showConfirmation`
- Открывается диалог ConfirmationDialog с соответствующим сообщением

**Что проверить в диалоге:**
- ✅ Заголовок и сообщение отображаются корректно
- ✅ Тип диалога влияет на цвет кнопки подтверждения (danger = красный, warning = желтый, default = синий)
- ✅ Кнопка "Отмена" закрывает диалог
- ✅ Кнопка подтверждения вызывает `onConfirm`
- ✅ Escape закрывает диалог
- ✅ Ctrl+Enter / Cmd+Enter подтверждает действие
- ✅ Клик вне диалога закрывает его
- ✅ Фокус правильно управляется (возвращается на предыдущий элемент после закрытия)

### 2. Тестирование инструмента showExcelFile

**Предварительно:**
- Загрузите тестовый Excel файл через API (`POST /api/excel/upload`) или через компонент ExcelUpload
- Запомните `fileId` из ответа (обычно это число, например: 1, 2, 3...)

**Шаги:**
1. Убедитесь, что файл загружен и вы знаете его `fileId`
2. В чате попросите AI показать файл, указав ID:
   - "Покажи файл с ID 1"
   - "Открой Excel файл номер 1"

**Ожидаемый результат:**
- AI вызывает tool `showExcelFile` с параметром `fileId`
- Открывается ExcelViewer с указанным файлом
- Файл отображается корректно

### 3. Тестирование инструмента highlightExcelCell

**Предварительно:**
- Загрузите тестовый Excel файл и узнайте его `fileId`
- Откройте файл в ExcelViewer или знайте, какие ячейки в нем есть (например, A1, B2, C3)

**Шаги:**
1. Убедитесь, что Excel файл загружен и открыт
2. В чате попросите AI выделить конкретную ячейку, указав её координаты:
   - "Покажи ячейку A1"
   - "Что в ячейке B5?"
   - "Выдели ячейку C10"

**Ожидаемый результат:**
- AI вызывает tool `highlightExcelCell` с параметрами `fileId` и `cellRef`
- Открывается ExcelViewer с нужным файлом
- Указанная ячейка автоматически выделяется
- Если ячейка не на текущей странице, происходит переход на нужную страницу

### 4. Тестирование комбинированных сценариев

**Сценарий 1: Показ ячейки в уже открытом файле**
1. Откройте Excel файл через `showExcelFile`
2. Попросите AI выделить другую ячейку в том же файле

**Ожидаемый результат:**
- ExcelViewer обновляется с новым выделением
- Переход на нужную страницу, если требуется

**Сценарий 2: Подтверждение действия с Excel**
1. Попросите AI удалить файл или изменить данные
2. AI должен запросить подтверждение
3. Подтвердите действие

**Ожидаемый результат:**
- Диалог подтверждения открывается
- После подтверждения действие выполняется (или логируется, если логика еще не реализована)

---

## Тестирование диалога подтверждения

### Ручное тестирование компонента

Вы можете протестировать компонент ConfirmationDialog напрямую, создав тестовую страницу:

```tsx
import ConfirmationDialog from '@/components/ConfirmationDialog';
import { useState } from 'react';

export default function TestPage() {
  const [isOpen, setIsOpen] = useState(false);

  return (
    <div>
      <button onClick={() => setIsOpen(true)}>Открыть диалог</button>
      <ConfirmationDialog
        isOpen={isOpen}
        title="Тестовое подтверждение"
        message="Это тестовое сообщение для проверки диалога"
        type="danger"
        confirmText="Удалить"
        cancelText="Отмена"
        onConfirm={() => {
          console.log('Подтверждено');
          setIsOpen(false);
        }}
        onCancel={() => {
          console.log('Отменено');
          setIsOpen(false);
        }}
      />
    </div>
  );
}
```

**Что проверить:**
- ✅ Все варианты типа диалога (default, danger, warning)
- ✅ Кастомизация текстов кнопок
- ✅ Клавиатурная навигация
- ✅ Доступность (ARIA атрибуты)
- ✅ Правильное управление фокусом

---

## Тестирование меншонов ячеек

### Тест 1: Автоматическое обнаружение меншонов

**Шаги:**
1. Откройте чат
2. Отправьте сообщение с упоминанием ячейки:
   - "Посмотри на ячейку A1"
   - "В B5 находится значение 100"
   - "Обнови данные в ячейке AA10"

**Ожидаемый результат:**
- Ячейки в формате Excel (буква + цифра, например A1, B5, AA10) автоматически обнаруживаются
- Они отображаются как кликабельные элементы (синий фон, подчеркивание при наведении)
- При наведении показывается подсказка с текстом ячейки

### Тест 2: Клик по меншону

**Шаги:**
1. Убедитесь, что Excel файл открыт в ExcelViewer
2. В сообщении с меншоном кликните на упоминание ячейки (например, A1)

**Ожидаемый результат:**
- Если файл открыт, соответствующая ячейка выделяется в ExcelViewer
- Если файл не открыт, можно добавить логику для его автоматического открытия (опционально)

### Тест 3: Множественные меншоны в одном сообщении

**Шаги:**
1. Отправьте сообщение с несколькими меншонами:
   - "Сравни ячейки A1, B2 и C3"

**Ожидаемый результат:**
- Все меншоны корректно обнаруживаются и отображаются
- Каждый меншон кликабелен независимо

### Тест 4: Меншоны в сообщениях ассистента

**Шаги:**
1. Попросите AI упомянуть ячейки в своем ответе:
   - "Какие ячейки содержат наибольшие значения?"

**Ожидаемый результат:**
- Если AI упоминает ячейки в формате Excel, они автоматически преобразуются в меншоны
- Меншоны кликабельны и функциональны

---

## Интеграционное тестирование

### Сценарий 1: Полный цикл работы с Excel

**Шаги:**
1. Загрузите Excel файл через API или UI
2. В чате попросите AI открыть файл
3. Попросите AI выделить конкретную ячейку
4. Попросите AI упомянуть ячейку в ответе
5. Кликните на меншон в ответе AI

**Ожидаемый результат:**
- Все шаги выполняются корректно
- Состояние между компонентами синхронизировано
- Нет ошибок в консоли

### Сценарий 2: Удаление файла с подтверждением

**Шаги:**
1. Загрузите Excel файл
2. В чате попросите AI удалить файл
3. Подтвердите удаление в диалоге
4. Попробуйте открыть удаленный файл

**Ожидаемый результат:**
- Диалог подтверждения открывается
- После подтверждения файл удаляется
- Попытка открыть удаленный файл возвращает ошибку 404

### Сценарий 3: Работа с большим файлом

**Шаги:**
1. Загрузите Excel файл с большим количеством строк (например, > 1000)
2. Откройте его в ExcelViewer
3. Проверьте пагинацию
4. Попросите AI выделить ячейку на другой странице

**Ожидаемый результат:**
- Файл загружается без проблем
- Пагинация работает корректно
- При запросе ячейки происходит автоматический переход на нужную страницу
- Производительность приемлемая (нет долгих задержек)

---

## Автоматизированное тестирование

### Unit тесты

Для новых компонентов можно создать unit тесты:

```bash
# Запуск всех тестов
npm test

# Тесты конкретного файла
npm test -- ExcelViewer.test.tsx
npm test -- ConfirmationDialog.test.tsx
```

### E2E тесты

Для E2E тестирования с Playwright:

```bash
# Запуск E2E тестов
npm run test:e2e

# Запуск с UI
npm run test:e2e:ui
```

**Примеры тестов, которые стоит добавить:**
- Загрузка Excel файла через UI
- Открытие ExcelViewer
- Выделение ячейки
- Использование client tools через чат
- Работа с диалогом подтверждения

---

## Проверка производительности

### Тест 1: Большие файлы

1. Загрузите файл с 10,000+ строками
2. Проверьте время загрузки
3. Проверьте время отображения в ExcelViewer
4. Проверьте работу пагинации

**Ожидаемый результат:**
- Загрузка файла < 5 секунд для файлов до 10MB
- Отображение первой страницы < 1 секунды
- Плавная навигация между страницами

### Тест 2: Множественные файлы

1. Загрузите 10+ файлов
2. Проверьте список файлов
3. Откройте разные файлы по очереди

**Ожидаемый результат:**
- Список файлов загружается быстро
- Переключение между файлами работает без проблем
- Нет утечек памяти

---

## Отладка

### Проблемы и решения

**Проблема: Tools не вызываются**
- Проверьте, что tools подключены в `app/api/chat/route.ts`
- Проверьте консоль браузера на наличие ошибок
- Убедитесь, что используемая модель OpenAI поддерживает function calling (gpt-3.5-turbo или новее)

**Проблема: Excel файл не парсится**
- Проверьте формат файла (должен быть .xlsx, .xls, или .csv)
- Проверьте логи сервера на наличие ошибок парсинга
- Убедитесь, что библиотека `xlsx` установлена

**Проблема: Диалог не открывается**
- Проверьте, что `onToolCall` вызывается в `useChat`
- Проверьте состояние `confirmationDialog` в React DevTools
- Проверьте консоль на наличие ошибок

**Проблема: Меншоны не отображаются**
- Проверьте регулярное выражение для обнаружения ячеек
- Убедитесь, что формат ячейки правильный (буква + цифра, например A1, не 1A)
- Проверьте консоль на наличие ошибок рендеринга

---

## Чек-лист тестирования

Перед завершением проверьте все пункты:

- [ ] API загрузки Excel файлов работает
- [ ] API получения данных Excel работает
- [ ] API удаления файла работает
- [ ] API списка файлов работает
- [ ] ExcelViewer отображает файлы корректно
- [ ] Выделение ячеек работает
- [ ] Пагинация работает для больших файлов
- [ ] ExcelUpload загружает файлы
- [ ] Client tools вызываются AI
- [ ] showConfirmation открывает диалог
- [ ] showExcelFile открывает файл
- [ ] highlightExcelCell выделяет ячейку
- [ ] ConfirmationDialog работает корректно
- [ ] Меншоны обнаруживаются в сообщениях
- [ ] Клик по меншону работает
- [ ] Интеграционные сценарии работают
- [ ] Производительность приемлемая
- [ ] Нет ошибок в консоли
- [ ] Доступность (a11y) работает

---

## Полезные команды

```bash
# Запуск сервера разработки
npm run dev

# Запуск тестов
npm test

# Запуск E2E тестов
npm run test:e2e

# Проверка типов TypeScript
npx tsc --noEmit

# Линтинг
npm run lint

# Проверка покрытия тестами
npm run test:coverage
```

---

## Контакты и поддержка

При возникновении проблем:
1. Проверьте логи сервера и консоль браузера
2. Убедитесь, что все зависимости установлены
3. Проверьте, что база данных инициализирована
4. См. также [TROUBLESHOOTING.md](./TROUBLESHOOTING.md) для общих проблем

