# Документация: npm run test:new-features

## Обзор

`npm run test:new-features` - это автоматизированный скрипт для полного тестирования новых функций ChatFlow. Он выполняет комплексную проверку всех компонентов системы, включая зависимости, базу данных, TypeScript типы, API эндпоинты и unit-тесты.

## Что делает скрипт

Скрипт последовательно выполняет 6 основных шагов тестирования:

### Шаг 1: Проверка зависимостей

**Что проверяется:**
- Наличие и версия Node.js
- Наличие и версия npm
- Установлены ли зависимости проекта (проверка наличия `node_modules`)

**Результат:**
- ✅ Если все зависимости установлены - продолжает выполнение
- ❌ Если Node.js или npm отсутствуют - скрипт останавливается с ошибкой
- 📦 Если `node_modules` отсутствует - автоматически запускает `npm install`

**Что это означает:**
Этот шаг гарантирует, что у вас есть все необходимое для запуска тестов и самого приложения.

---

### Шаг 2: Создание тестового Excel файла

**Что проверяется:**
- Запускает скрипт `npm run create-test-excel`
- Генерирует файл `test-data.xlsx` с тестовыми данными

**Что создается:**
Файл `test-data.xlsx` содержит пример данных о сотрудниках с колонками:
- ID
- Name (Имя)
- Department (Отдел)
- Salary (Зарплата)
- Hire Date (Дата найма)

**Зачем это нужно:**
Тестовый Excel файл необходим для проверки функциональности работы с Excel файлами:
- Загрузка файлов
- Парсинг данных
- Отображение данных в интерфейсе
- API для работы с Excel

**Результат:**
- ✅ Файл успешно создан - тест пройден
- ❌ Ошибка при создании - тест провален

---

### Шаг 3: Проверка базы данных

**Что проверяется:**
- Выполняется `npm run migrate` для применения миграций базы данных
- Проверяется, что структура БД соответствует текущей схеме

**Особенности:**
- Этот шаг **не критичен** для основной функциональности
- Если миграции уже применены, шаг помечается как пропущенный (SKIP), но не считается ошибкой
- Если скрипт миграций отсутствует, шаг также пропускается

**Что это делает:**
Применяет SQL миграции из папки `migrations/` для создания/обновления структуры базы данных SQLite.

**Результат:**
- ✅ Миграции успешно применены - тест пройден
- ⚠️ Миграции уже применены или скрипт отсутствует - пропущено (не ошибка)

---

### Шаг 4: Проверка TypeScript типов

**Что проверяется:**
- Запускается `npx tsc --noEmit` для проверки типов TypeScript
- Проверяет все `.ts` и `.tsx` файлы на наличие ошибок типизации

**Особенности:**
- Этот шаг **не критичен** для runtime выполнения
- Предупреждения TypeScript не приводят к провалу теста
- Результат показывается как WARNING, если есть ошибки типов

**Что это проверяет:**
- Корректность типов во всем проекте
- Соответствие типов импортов и экспортов
- Отсутствие ошибок компиляции TypeScript

**Результат:**
- ✅ Ошибок типов нет - тест пройден
- ⚠️ Найдены ошибки типов - показано предупреждение (не критично)

**Примечание:** Некоторые предупреждения могут быть связаны с особенностями Next.js 16 (например, `params` может быть Promise в route handlers) и не влияют на работу приложения.

---

### Шаг 5: Запуск сервера и тестирование API

**Что проверяется:**

#### 5.1. Проверка состояния сервера
- Проверяется, запущен ли сервер разработки на `http://localhost:3000`
- Если сервер не запущен, скрипт пытается запустить его автоматически

#### 5.2. Автоматический запуск сервера (если нужно)
- Находит `npm` в системном PATH
- Запускает `npm run dev` в фоновом режиме
- Ждет до 60 секунд, пока сервер не станет доступен
- Показывает прогресс каждые 10 секунд

#### 5.3. Тестирование Excel API
- Запускает `node scripts/test-excel-api.js`
- Выполняет следующие API запросы:
  1. **POST /api/excel/upload** - загрузка тестового Excel файла
  2. **GET /api/excel/list** - получение списка всех загруженных файлов
  3. **GET /api/excel/[fileId]** - получение данных конкретного файла с пагинацией
  4. **DELETE /api/excel/[fileId]** - удаление файла

#### 5.4. Остановка сервера
- Если сервер был запущен скриптом, он автоматически останавливается после тестов
- Если сервер был уже запущен до скрипта, он остается работать

**Результат:**
- ✅ Все API запросы выполнены успешно - тест пройден
- ⚠️ Сервер не запустился в течение 60 секунд - тесты API пропущены (не критично)
- ❌ API запросы завершились с ошибками - тест провален

**Что проверяет API тест:**
- Корректность работы эндпоинтов загрузки Excel
- Парсинг Excel файлов
- Хранение метаданных файлов в БД
- Получение списка файлов
- Пагинация данных
- Удаление файлов

---

### Шаг 6: Unit тесты

**Что проверяется:**
- Запускается `npm test` для выполнения всех unit тестов
- Используется флаг `--passWithNoTests` для корректной работы при отсутствии тестов

**Что тестируется:**
- **API Routes:**
  - `/api/chat` - обработка чат запросов, валидация, обработка ошибок
  - `/api/messages` - работа с сообщениями
  - `/api/threads` - работа с тредами
  - `/api/excel/*` - тесты Excel API (если есть)

- **Components:**
  - `ChatInterface` - основной интерфейс чата
  - `ThreadList` - список тредов
  - `ExcelViewer` - просмотр Excel файлов
  - `ConfirmationDialog` - диалоги подтверждения

- **Library functions:**
  - `excel-service` - сервис для работы с Excel
  - `error-handler` - обработка ошибок
  - `retry` - механизм повторных попыток
  - `migration-manager` - управление миграциями БД
  - `db` - работа с базой данных

**Результат:**
- ✅ Все тесты пройдены - тест пройден
- ❌ Есть проваленные тесты - тест провален
- Показывается детальная информация о проваленных тестах

**Пример вывода:**
```
Test Suites: 11 passed, 11 total
Tests:       116 passed, 116 total
```

---

## Итоговая статистика

В конце выполнения скрипт выводит итоговую статистику:

```
Total tests: 5
Passed: 5
Failed: 0
Success rate: 100%
```

**Где:**
- **Total tests** - количество выполненных основных шагов
- **Passed** - количество успешно пройденных шагов
- **Failed** - количество проваленных шагов
- **Success rate** - процент успешных тестов

**Итоговый результат:**
- ✅ `[SUCCESS] All tests passed!` - все тесты пройдены, скрипт завершается с кодом 0
- ❌ `[FAILURE] Some tests failed.` - есть проваленные тесты, скрипт завершается с кодом 1

---

## Когда использовать

### Рекомендуется запускать:

1. **После добавления новых функций** - чтобы убедиться, что ничего не сломалось
2. **Перед коммитом** - для проверки работоспособности кода
3. **Перед деплоем** - для финальной проверки всех компонентов
4. **После обновления зависимостей** - для проверки совместимости
5. **После изменений в API** - для проверки корректности работы эндпоинтов

### Не обязательно запускать:

- При работе только с UI компонентами (можно использовать `npm test` напрямую)
- Если сервер уже запущен и вы хотите протестировать только API (используйте `npm run test:excel-api`)

---

## Требования

### Обязательные:
- Node.js (проверяется версия)
- npm (проверяется версия)
- Установленные зависимости проекта (`npm install`)

### Опциональные:
- Запущенный сервер разработки (если не запущен, скрипт попытается запустить автоматически)
- База данных SQLite (создается автоматически при миграциях)

---

## Время выполнения

Примерное время выполнения скрипта:
- **Зависимости:** < 1 секунды
- **Создание Excel файла:** < 1 секунды
- **Миграции БД:** 1-2 секунды
- **TypeScript проверка:** 5-15 секунд
- **Запуск сервера:** 10-60 секунд (если не запущен)
- **API тесты:** 2-5 секунд
- **Unit тесты:** 5-15 секунд

**Общее время:** примерно 30-100 секунд в зависимости от скорости системы и необходимости запуска сервера.

---

## Возможные проблемы и решения

### Проблема: "Server did not start within 60 seconds"

**Причина:** Сервер разработки не смог запуститься в течение 60 секунд.

**Решение:**
1. Запустите сервер вручную: `npm run dev`
2. Дождитесь появления сообщения "Ready"
3. Запустите скрипт снова

### Проблема: "TypeScript found some issues"

**Причина:** В коде есть ошибки или предупреждения типизации.

**Решение:**
- Если это предупреждения (не ошибки) - можно игнорировать, они не критичны для runtime
- Если это ошибки - исправьте их перед коммитом
- Некоторые предупреждения связаны с особенностями Next.js 16 и могут быть проигнорированы

### Проблема: "Migration failed"

**Причина:** Ошибка при применении миграций БД.

**Решение:**
- Проверьте, что файл БД не заблокирован другим процессом
- Убедитесь, что у процесса есть права на запись в директорию проекта
- Если БД уже инициализирована - это нормально, шаг будет пропущен

### Проблема: "API tests failed"

**Причина:** Один или несколько API эндпоинтов вернули ошибку.

**Решение:**
1. Проверьте логи сервера разработки
2. Убедитесь, что сервер запущен и работает корректно
3. Проверьте, что тестовый Excel файл (`test-data.xlsx`) существует
4. Проверьте подключение к базе данных

### Проблема: "Unit tests failed"

**Причина:** Один или несколько unit тестов провалились.

**Решение:**
1. Посмотрите детальный вывод тестов для информации о провале
2. Исправьте код или тесты в соответствии с ошибками
3. Запустите конкретный тест: `npm test -- __tests__/path/to/test.test.ts`

---

## Альтернативные команды

Если вы хотите запустить только часть проверок:

- **Только unit тесты:** `npm test`
- **Только API тесты (сервер должен быть запущен):** `npm run test:excel-api`
- **Только создание тестового Excel файла:** `npm run create-test-excel`
- **Только TypeScript проверка:** `npx tsc --noEmit`
- **Только миграции:** `npm run migrate`

---

## Платформы

Скрипт доступен для разных платформ:

- **Windows:** `npm run test:new-features` (использует PowerShell скрипт)
- **Linux/macOS:** `npm run test:new-features:bash` (использует Bash скрипт)

Оба скрипта выполняют одинаковые проверки, но адаптированы под соответствующие оболочки.

---

## Интеграция в CI/CD

Скрипт можно использовать в CI/CD пайплайнах:

```yaml
# Пример для GitHub Actions
- name: Run full feature tests
  run: npm run test:new-features
```

**Примечание:** В CI/CD окружениях сервер обычно запускается отдельным шагом, поэтому автоматический запуск сервера в скрипте может быть не нужен.

---

## Дополнительная информация

- Подробные инструкции по тестированию новых функций: `docs/TESTING_NEW_FEATURES.md`
- Устранение неполадок: `docs/TROUBLESHOOTING.md`
- Общая документация проекта: `README.md`



